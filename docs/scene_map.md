# Scene Map

No Godot scene files are currently present in the repository.

This document must be rebuilt from the actual `.tscn` and `.scn` files after the complete project is uploaded.

Expected audit format:

```text
Main scene
└── Level
    ├── Player
    ├── Enemies
    ├── Camera
    ├── UI
    └── Transition systems
```

Do not populate this map from memory, design documents or earlier audits.
